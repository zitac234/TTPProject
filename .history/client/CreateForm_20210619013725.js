import React from 'react'
import axios from 'axios'
import Form  from './Form'

export default class CreateForm = (props) => {
      const [link, setLink] = useState('')
      const [text, settext] = useState('')

}