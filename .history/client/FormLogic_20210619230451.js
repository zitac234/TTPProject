import { text } from 'body-parser'
import Form from './Form'
const Sentence = (props) => {
      // const[text, setText] = useState('')
      // const [search, setSearch] = useState('')
      let text
return (
      <Form text={text} serach={search}/>
)
}
